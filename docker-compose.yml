services:
  pm_sim:
    build: .
    working_dir: /app
    volumes:
      # Mount the entire project root so all script files are available
      - .:/app
    # Default thread files
    command: test_complex.txt
